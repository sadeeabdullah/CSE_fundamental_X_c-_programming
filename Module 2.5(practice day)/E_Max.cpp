#include <bits/stdc++.h>
using namespace std;
int main(){
    int x;
    cin >> x;
    int arr[x];
    //taking input of the array element
    for (int i = 0; i < x; i++)
    {
        cin >> arr[i];
    }
    
    int max_element = *max_element(arr.begin(), arr)
   cout << 
    
    return 0;
}